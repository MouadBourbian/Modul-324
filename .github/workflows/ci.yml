name: CI Pipeline

on:
  push:
    branches: [ main, develop, copilot/** ]
  pull_request:
    branches: [ main, develop ]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  lint-and-test:
    name: Lint and Test Backend
    runs-on: ubuntu-latest
    
    # MongoDB service container for tests
    services:
      mongodb:
        image: mongo:7.0
        env:
          MONGO_INITDB_ROOT_USERNAME: testuser
          MONGO_INITDB_ROOT_PASSWORD: testpassword
        ports:
          - 27017:27017
        options: >-
          --health-cmd "mongosh --eval 'db.runCommand({ ping: 1 })'"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    defaults:
      run:
        working-directory: ./ticketsystem/backend

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: ./ticketsystem/backend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Run ESLint
        run: npm run lint

      - name: Run tests
        env:
          MONGO_URL: mongodb://testuser:testpassword@localhost:27017/ticketsystem-test?authSource=admin
          NODE_ENV: test
        run: npm test

  # Placeholder for future deployment
  # This job is commented out and will be implemented when deployment is ready
  # deploy:
  #   name: Deploy to Production
  #   runs-on: ubuntu-latest
  #   needs: lint-and-test
  #   if: github.ref == 'refs/heads/main' && github.event_name == 'push'
  #   
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4
  #     
  #     - name: Setup Node.js
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: '20'
  #     
  #     # Future deployment steps:
  #     # 1. Build the application (if needed)
  #     # 2. Deploy to hosting service (e.g., Heroku, AWS, Azure, etc.)
  #     # 3. Use MongoDB Atlas connection string from GitHub Secrets
  #     #    Environment variable: MONGO_URL (should be added to GitHub Secrets)
  #     # 4. Run database migrations (if needed)
  #     # 5. Health check after deployment
  #     
  #     - name: Deploy (Placeholder)
  #       run: echo "Deployment will be configured here"
